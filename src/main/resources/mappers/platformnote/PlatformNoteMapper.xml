<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iqkv.foundation.iamservice.platformnote.PlatformNoteMapper">

    <resultMap id="platformNoteResultMap" type="com.iqkv.foundation.iamservice.platformnote.PlatformNote">
        <id  property="id"         column="id"/>
        <result property="title"      column="title"/>
        <result property="body"       column="body"/>
        <result property="severity"   column="severity"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="status"     column="status"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="createdBy"  column="created_by"/>
        <result property="createdAt"  column="created_at"/>
        <result property="updatedAt"  column="updated_at"/>
    </resultMap>

    <!-- ── Write operations ──────────────────────────────────────────────── -->

    <insert id="insert" parameterType="com.iqkv.foundation.iamservice.platformnote.PlatformNote">
        INSERT INTO public.platform_notes
            (id, title, body, severity, status, created_by, created_at, updated_at)
        VALUES
            (#{id},
             #{title},
             #{body},
             #{severity,  typeHandler=org.apache.ibatis.type.EnumTypeHandler},
             #{status,    typeHandler=org.apache.ibatis.type.EnumTypeHandler},
             #{createdBy},
             #{createdAt},
             #{updatedAt})
    </insert>

    <update id="update" parameterType="com.iqkv.foundation.iamservice.platformnote.PlatformNote">
        UPDATE public.platform_notes
        SET title      = #{title},
            body       = #{body},
            severity   = #{severity,  typeHandler=org.apache.ibatis.type.EnumTypeHandler},
            status     = #{status,    typeHandler=org.apache.ibatis.type.EnumTypeHandler},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM public.platform_notes
        WHERE id = #{id}
    </delete>

    <!-- ── Read operations ───────────────────────────────────────────────── -->

    <select id="findById" resultMap="platformNoteResultMap">
        SELECT id, title, body, severity, status, created_by, created_at, updated_at
        FROM   public.platform_notes
        WHERE  id = #{id}
    </select>

    <select id="findAll" resultMap="platformNoteResultMap">
        SELECT id, title, body, severity, status, created_by, created_at, updated_at
        FROM   public.platform_notes
        <where>
            <if test="search != null">
                (title ILIKE '%' || #{search} || '%'
                 OR body ILIKE '%' || #{search} || '%')
            </if>
            <if test="severity != null">
                AND severity = #{severity}::VARCHAR
            </if>
            <if test="status != null">
                AND status = #{status}::VARCHAR
            </if>
        </where>
        ORDER BY
            <choose>
                <when test="sortBy == 'title'"     >title</when>
                <when test="sortBy == 'severity'"  >severity</when>
                <when test="sortBy == 'status'"    >status</when>
                <when test="sortBy == 'updated_at'">updated_at</when>
                <otherwise>created_at</otherwise>
            </choose>
            <choose>
                <when test="sortDir == 'ASC'">ASC</when>
                <otherwise>DESC</otherwise>
            </choose>
        LIMIT  #{limit}
        OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM   public.platform_notes
        <where>
            <if test="search != null">
                (title ILIKE '%' || #{search} || '%'
                 OR body ILIKE '%' || #{search} || '%')
            </if>
            <if test="severity != null">
                AND severity = #{severity}::VARCHAR
            </if>
            <if test="status != null">
                AND status = #{status}::VARCHAR
            </if>
        </where>
    </select>

</mapper>
