<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iqkv.foundation.iamservice.user.UserMapper">

    <resultMap id="userResultMap" type="com.iqkv.foundation.iamservice.user.User">
        <id     property="id"                   column="id"/>
        <result property="email"                column="email"/>
        <result property="passwordHash"         column="password_hash"/>
        <result property="firstName"            column="first_name"/>
        <result property="lastName"             column="last_name"/>
        <result property="status"               column="status"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="emailVerified"        column="email_verified"/>
        <result property="lastGlobalSignoutAt"  column="last_global_signout_at"/>
        <result property="createdAt"            column="created_at"/>
        <result property="updatedAt"            column="updated_at"/>
        <result property="createdBy"            column="created_by"/>
        <result property="updatedBy"            column="updated_by"/>
    </resultMap>

    <insert id="upsertByEmail" parameterType="com.iqkv.foundation.iamservice.user.User">
        INSERT INTO public.users
            (id, email, password_hash, first_name, last_name, status,
             email_verified, created_at, updated_at, created_by, updated_by)
        VALUES
            (#{id}, #{email}, #{passwordHash}, #{firstName}, #{lastName},
             #{status, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
             #{emailVerified}, #{createdAt}, #{updatedAt}, #{createdBy}, #{updatedBy})
        ON CONFLICT (email) DO NOTHING
    </insert>

    <select id="findById" resultMap="userResultMap">
        SELECT id, email, password_hash, first_name, last_name, status,
               email_verified, last_global_signout_at,
               created_at, updated_at, created_by, updated_by
        FROM public.users
        WHERE id = #{id}
    </select>

    <select id="findByEmail" resultMap="userResultMap">
        SELECT id, email, password_hash, first_name, last_name, status,
               email_verified, last_global_signout_at,
               created_at, updated_at, created_by, updated_by
        FROM public.users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS (SELECT 1 FROM public.users WHERE email = #{email})
    </select>

    <resultMap id="userWithOrganizationsResultMap"
               type="com.iqkv.foundation.iamservice.user.UserWithOrganizations">
        <id     property="id"            column="id"/>
        <result property="email"         column="email"/>
        <result property="firstName"     column="first_name"/>
        <result property="lastName"      column="last_name"/>
        <result property="status"        column="status"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="emailVerified" column="email_verified"/>
        <result property="createdAt"     column="created_at"/>
        <result property="updatedAt"     column="updated_at"/>
        <result property="organizations" column="organizations"
                typeHandler="com.iqkv.foundation.iamservice.infrastructure.mybatis.StringArrayTypeHandler"/>
    </resultMap>

    <select id="findAll" resultMap="userWithOrganizationsResultMap">
        SELECT u.id,
               u.email,
               u.first_name,
               u.last_name,
               u.status,
               u.email_verified,
               u.created_at,
               u.updated_at,
               array_agg(t.name ORDER BY t.name)
                   FILTER (WHERE t.name IS NOT NULL) AS organizations
        FROM public.users u
        LEFT JOIN public.tenant_memberships m
               ON m.user_id = u.id
        LEFT JOIN public.tenants t
               ON t.tenant_key = m.tenant_key
        <where>
            <if test="search != null and search != ''">
                (
                    LOWER(u.email)      LIKE LOWER(CONCAT('%', #{search}, '%'))
                 OR LOWER(u.first_name) LIKE LOWER(CONCAT('%', #{search}, '%'))
                 OR LOWER(u.last_name)  LIKE LOWER(CONCAT('%', #{search}, '%'))
                )
            </if>
            <if test="status != null and status != ''">
                AND u.status = #{status}::account_status
            </if>
        </where>
        GROUP BY u.id, u.email, u.first_name, u.last_name, u.status,
                 u.email_verified, u.created_at, u.updated_at
        ORDER BY
            <choose>
                <when test="sortBy == 'email'">u.email</when>
                <when test="sortBy == 'firstName'">u.first_name</when>
                <when test="sortBy == 'lastName'">u.last_name</when>
                <when test="sortBy == 'updatedAt'">u.updated_at</when>
                <otherwise>u.created_at</otherwise>
            </choose>
            <choose>
                <when test="sortDir == 'asc'">ASC</when>
                <otherwise>DESC</otherwise>
            </choose>
            , u.id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM public.users
        <where>
            <if test="search != null and search != ''">
                (
                    LOWER(email)      LIKE LOWER(CONCAT('%', #{search}, '%'))
                 OR LOWER(first_name) LIKE LOWER(CONCAT('%', #{search}, '%'))
                 OR LOWER(last_name)  LIKE LOWER(CONCAT('%', #{search}, '%'))
                )
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}::account_status
            </if>
        </where>
    </select>

    <delete id="deleteById">
        DELETE FROM public.users WHERE id = #{id}
    </delete>

    <update id="update" parameterType="com.iqkv.foundation.iamservice.user.User">
        UPDATE public.users
        SET first_name = #{firstName},
            last_name  = #{lastName},
            status     = #{status, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <update id="updateLastGlobalSignoutAt">
        UPDATE public.users
        SET last_global_signout_at = #{lastGlobalSignoutAt}
        WHERE id = #{userId}
    </update>

    <select id="findLastGlobalSignoutAt" resultType="java.time.Instant">
        SELECT last_global_signout_at
        FROM public.users
        WHERE id = #{userId}
    </select>

    <update id="setEmailVerified">
        UPDATE public.users
        SET email_verified = true
        WHERE id = #{userId}
    </update>

    <update id="updatePassword">
        UPDATE public.users
        SET password_hash = #{passwordHash},
            updated_at    = #{updatedAt}
        WHERE id = #{userId}
    </update>

</mapper>
