<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iqkv.foundation.iamservice.oauth2.mapper.TenantOidcProviderMapper">

    <resultMap id="TenantOidcProviderResultMap" type="com.iqkv.foundation.iamservice.oauth2.TenantOidcProvider">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="providerKey" column="provider_key"/>
        <result property="displayName" column="display_name"/>
        <result property="issuerUri" column="issuer_uri"/>
        <result property="clientId" column="client_id"/>
        <result property="clientSecret" column="client_secret"/>
        <result property="scopes" column="scopes"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByProviderKey" resultMap="TenantOidcProviderResultMap">
        SELECT id, tenant_id, provider_key, display_name, issuer_uri,
               client_id, client_secret, scopes, enabled, created_at, updated_at
        FROM public.tenant_oidc_providers
        WHERE provider_key = #{providerKey}
        AND enabled = true
    </select>

    <select id="findByTenantId" resultMap="TenantOidcProviderResultMap">
        SELECT id, tenant_id, provider_key, display_name, issuer_uri,
               client_id, client_secret, scopes, enabled, created_at, updated_at
        FROM public.tenant_oidc_providers
        WHERE tenant_id = #{tenantId}
    </select>

    <insert id="insert" parameterType="com.iqkv.foundation.iamservice.oauth2.TenantOidcProvider">
        INSERT INTO public.tenant_oidc_providers (
            id, tenant_id, provider_key, display_name, issuer_uri,
            client_id, client_secret, scopes, enabled, created_at, updated_at
        ) VALUES (
            #{id}, #{tenantId}, #{providerKey}, #{displayName}, #{issuerUri},
            #{clientId}, #{clientSecret}, #{scopes}, #{enabled}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update" parameterType="com.iqkv.foundation.iamservice.oauth2.TenantOidcProvider">
        UPDATE public.tenant_oidc_providers
        <set>
            provider_key = #{providerKey},
            display_name = #{displayName},
            issuer_uri = #{issuerUri},
            client_id = #{clientId},
            <if test="clientSecret != null">
                client_secret = #{clientSecret},
            </if>
            scopes = #{scopes},
            enabled = #{enabled},
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteByTenantId">
        DELETE FROM public.tenant_oidc_providers
        WHERE tenant_id = #{tenantId}
    </delete>

</mapper>
