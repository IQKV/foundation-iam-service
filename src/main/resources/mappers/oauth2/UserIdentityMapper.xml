<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iqkv.foundation.iamservice.oauth2.mapper.UserIdentityMapper">

    <resultMap id="UserIdentityResultMap" type="com.iqkv.foundation.iamservice.oauth2.UserIdentity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="provider" column="provider"/>
        <result property="providerSub" column="provider_sub"/>
        <result property="email" column="email"/>
        <result property="displayName" column="display_name"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="linkedAt" column="linked_at"/>
        <result property="lastUsedAt" column="last_used_at"/>
    </resultMap>

    <select id="findByProviderAndProviderSub" resultMap="UserIdentityResultMap">
        SELECT id, user_id, provider, provider_sub, email, display_name, avatar_url, linked_at, last_used_at
        FROM public.user_identities
        WHERE provider = #{provider}
        AND provider_sub = #{providerSub}
    </select>

    <select id="findByUserId" resultMap="UserIdentityResultMap">
        SELECT id, user_id, provider, provider_sub, email, display_name, avatar_url, linked_at, last_used_at
        FROM public.user_identities
        WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.iqkv.foundation.iamservice.oauth2.UserIdentity">
        INSERT INTO public.user_identities (id, user_id, provider, provider_sub, email, display_name, avatar_url, linked_at, last_used_at)
        VALUES (#{id}, #{userId}, #{provider}, #{providerSub}, #{email}, #{displayName}, #{avatarUrl}, #{linkedAt}, #{lastUsedAt})
    </insert>

    <update id="updateLastUsedAt">
        UPDATE public.user_identities
        SET last_used_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM public.user_identities
        WHERE id = #{id}
    </delete>

    <delete id="deleteByUserIdAndProvider">
        DELETE FROM public.user_identities
        WHERE user_id = #{userId}
        AND provider = #{provider}
    </delete>

</mapper>
