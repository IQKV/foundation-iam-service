<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.33.xsd">

    <changeSet id="20260510000000" author="iqkv">
        <comment>
            Platform-level authortities keyed directly to users, bypassing tenant membership.
            PLATFORM_ADMIN is stored here — not in tenant_member_authorities.
        </comment>

        <createTable tableName="platform_authorities" schemaName="public">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_platform_authorities_user_id"
                             references="public.users(id)"
                             deleteCascade="true"/>
            </column>
            <column name="authority" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="granted_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="granted_by" type="VARCHAR(255)"/>
        </createTable>

        <addUniqueConstraint
                constraintName="uq_platform_authorities_user_authority"
                tableName="platform_authorities"
                schemaName="public"
                columnNames="user_id, authority"/>

        <createIndex indexName="idx_platform_authorities_user_id"
                     tableName="platform_authorities"
                     schemaName="public">
            <column name="user_id"/>
        </createIndex>
        
        <rollback>
            <dropIndex indexName="idx_platform_authorities_user_id" tableName="platform_authorities" schemaName="public"/>
            <dropUniqueConstraint constraintName="uq_platform_authorities_user_authority" tableName="platform_authorities" schemaName="public"/>
            <dropTable tableName="platform_authorities" schemaName="public"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
